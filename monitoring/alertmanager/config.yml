global:
  resolve_timeout: 5m

route:
  group_by: ['alertname']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 3h
  receiver: 'pager'

receivers:
  - name: 'pager'
    # For now, a null receiver. Add email, Slack, PagerDuty integrations here.
    # Example: email configs / webhook / slack attachments
