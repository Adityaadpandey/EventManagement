apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: tixin
spec:
  selector:
    app: backend
  ports:
    - name: http
      port: 7001
      targetPort: 7001
  type: NodePort
