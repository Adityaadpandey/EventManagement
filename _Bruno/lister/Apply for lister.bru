meta {
  name: Apply for lister
  type: http
  seq: 2
}

post {
  url: {{host}}/lister/apply
  body: json
  auth: inherit
}

body:json {
  {
    "companyName": "Can be edited",
    "bio": "......."
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
