meta {
  name: Apply for lister
  type: http
  seq: 2
}

post {
  url: {{host}}/lister/apply
  body: json
  auth: inherit
}

body:json {
  {
    "companyName": "Ai Adda",
    "bio": "......"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
