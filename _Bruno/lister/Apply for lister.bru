meta {
  name: Apply for lister
  type: http
  seq: 2
}

post {
  url: {{host}}/lister/apply
  body: json
  auth: inherit
}

body:json {
  {
    "companyName": "Alpha",
    "bio": "Noce"
  }
}

settings {
  encodeUrl: true
}
