meta {
  name: user tickets
  type: http
  seq: 7
}

get {
  url: {{host}}/ticket/my-tickets
  body: none
  auth: inherit
}

script:post-response {
  bru.setEnvVar("ticketId",res.body.data[0].ticketId)
}

settings {
  encodeUrl: true
}
