meta {
  name: update event
  type: http
  seq: 9
}

patch {
  url: {{host}}/event/{{eventId}}
  body: json
  auth: inherit
}

body:json {
  {
    "title":"good Question"
  }
}

settings {
  encodeUrl: true
}
