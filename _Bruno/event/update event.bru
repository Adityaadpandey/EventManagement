meta {
  name: update event
  type: http
  seq: 2
}

patch {
  url: {{host}}/event/{{eventId}}
  body: json
  auth: inherit
}

body:json {
  {
    "title":"good Question"
  }
}

settings {
  encodeUrl: true
}
