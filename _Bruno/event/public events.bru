meta {
  name: public events
  type: http
  seq: 5
}

get {
  url: {{host}}/event/public?page=1&limit=20
  body: none
  auth: none
}

params:query {
  page: 1
  limit: 20
}

script:post-response {
  bru.setEnvVar("eventId",res.body.data[0].eventId)
}

settings {
  encodeUrl: true
}
