meta {
  name: event update
  type: http
  seq: 7
}

post {
  url: {{host}}/event/{{eventId}}/info-update
  body: json
  auth: inherit
}

body:json {
  {
    "update":"New place is here"
  }
}

settings {
  encodeUrl: true
}
