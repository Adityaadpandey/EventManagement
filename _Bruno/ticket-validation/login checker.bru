meta {
  name: login checker
  type: http
  seq: 10
}

post {
  url: {{host}}/ticket-validation/checker/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "17064",
    "password": "9aa2f0cd"
  }
}

script:post-response {
  bru.setEnvVar("checker-token",res.body.data.token)
}

settings {
  encodeUrl: true
  timeout: 0
}
