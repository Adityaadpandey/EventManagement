meta {
  name: get profile
  type: http
  seq: 2
}

get {
  url: {{host}}/user/profile
  body: none
  auth: bearer
}

headers {
  : 
}

auth:bearer {
  token: {{token}}
}

script:post-response {
  bru.setEnvVar("token",res.body.data.token)
}

settings {
  encodeUrl: true
  timeout: 0
}
